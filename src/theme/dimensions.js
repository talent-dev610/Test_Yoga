import { hScaleRatio, wScale } from "../utils/scailing";

const dimensions = {
  paddingTop: hScaleRatio(56),
  paddingHPrimary: wScale(24),
  paddingHSecondary: wScale(52),
};

export default dimensions;
